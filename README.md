# aegirson.com

Starter scaffold for the personal website. Contains a minimal responsive HTML/CSS/JS site.

## Local development

```powershell
cd 'd:\Project\GitHub\www.aegirson.com'
# initialize and commit (if not already):
# git init
# git add .
# git commit -m "Initial website scaffold"
```

To publish, add the GitHub remote and push to the repository: `https://github.com/jimaegirson-pixel/-girson.com`.
